#include <stdio.h>
#include <stdlib.h>
int main()
{
    int propositions,clauses;
    scanf("%d %d",&propositions,&clauses);
    int bool[4];
    char strings[clauses][256];
    for(int i=0;i<clauses;i++)
    {
        while()      
    }
    return 0;
}